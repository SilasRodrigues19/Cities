import { BaseLayoutOfPages } from '../../shared/layouts';

export const Dashboard = () => {
  return (
    <BaseLayoutOfPages title="Página inicial" toolbar={<>Toolbar</>}>
      Testando
    </BaseLayoutOfPages>
  );
};
