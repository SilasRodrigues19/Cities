import { BaseLayoutOfPages } from '../../shared/layouts';
import { Divider } from '@mui/material';

export const Donate = () => {
  return (
    <BaseLayoutOfPages title="Donate">
      <Divider variant="middle" sx={{ mx: 2 }} />
    </BaseLayoutOfPages>
  );
};
